
import React from "react";
import SitesContainer from "@/components/sites/SitesContainer";

// Main Sites component - now much simpler as logic is extracted
const Sites = () => {
  return <SitesContainer />;
};

export default Sites;
