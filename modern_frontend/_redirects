# Netlify redirects file for SPA routing
# This ensures all routes are handled by React Router

/*    /index.html   200

# API proxy (optional - if you want to proxy API calls through Netlify)
# /api/*  https://infrastructure-management-system-production.up.railway.app/api/:splat  200
